# ── Web 框架 ────────────────────────────────────────────────
fastapi==0.139.0
starlette==1.3.1
uvicorn[standard]==0.51.0
pydantic==2.9.2
pydantic-settings==2.5.2
python-multipart==0.0.32
websockets==13.1

# ── HTTP 客户端 ─────────────────────────────────────────────
httpx==0.27.2
httpx-sse==0.4.0

# ── LLM SDK（仅 adapter 层 import，业务禁用） ──────────────
openai==1.51.0

# ── DB ──────────────────────────────────────────────────────
sqlalchemy[asyncio]==2.0.35
aiosqlite==0.20.0
alembic==1.13.3

# ── RAG ─────────────────────────────────────────────────────
jieba==0.42.1
rank-bm25==0.2.2
pdfplumber==0.11.10
markitdown==0.0.1a3          # MS 万能文档→markdown 抽取（该 alpha 版本不声明 extras）

# ── 音频 / STT 兜底 ─────────────────────────────────────────
soundfile==0.12.1
numpy==2.1.1
# funasr 已删除：原本是 SenseVoice in-process fallback 的依赖；PR
# `echodesk-remove-sensevoice` 把 SenseVoice (含 :8093 GPU + in-process)
# 全砍掉后，唯一 STT 走 firered HTTP（不需要本地模型），funasr 不再使用。
torch==2.11.0             # CPU 版即可（ECAPA 声纹用；与 torchaudio 同版本）
torchaudio==2.11.0
speechbrain==1.1.0        # ECAPA-TDNN 声纹
huggingface_hub==1.23.0
# modelscope 不应被引入（远程 GPU 已部署模型）。

# ── Web Search ──────────────────────────────────────────────
tavily-python==0.5.0
duckduckgo-search==6.2.13

# ── Skill 产物生成（adapter 层 only） ──────────────────────
python-docx==1.1.2
openpyxl==3.1.5
jinja2==3.1.6             # HTML 模板
fpdf2==2.8.7              # PDF 生成（P4-M3，纯 Python 无原生依赖；中文需 Noto Sans SC TTF）

# ── 观测 / 日志 ─────────────────────────────────────────────
structlog==24.4.0
python-json-logger==2.0.7

# ── 工具链 ──────────────────────────────────────────────────
tenacity==9.0.0           # retry
anyio==4.6.0
